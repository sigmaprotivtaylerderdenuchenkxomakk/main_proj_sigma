from sqlite3 import *


def creare_db():
    con = connect("users.db")
    create = con.execute(
        """
    CREATE TABLE IF NOT EXISTS user_data(
        id INTEGER PRIMARY KEY,
        user_id TEXT,
        chat_id TEXT

    )
"""
    )




















































































































































